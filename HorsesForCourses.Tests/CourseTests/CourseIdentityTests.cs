using HorsesForCourses.Domain.Courses;

namespace HorsesForCourses.Tests.CourseTests;

public class CourseIdentityTests
{
    /*
    // Defect 4
    [Fact]
    public void TwoCourses_With_TheSameNameAndPeriod_AreEqual()
    {
        Course course1 = Course.Create("C#", new DateOnly(2026, 1, 5), new DateOnly(2026, 1, 30));
        Course course2 = Course.Create("C#", new DateOnly(2026, 1, 5), new DateOnly(2026, 1, 30));
        Assert.Equal(course1, course2);
    }

    [Fact]
    public void TowCourses_With_DifferentPeriods_AreNotEqual()
    {
        Course course1 = Course.Create("C#", new DateOnly(2026, 1, 5), new DateOnly(2026, 1, 30));
        Course course2 = Course.Create("C#", new DateOnly(2026, 2, 1), new DateOnly(2026, 2, 28));
        Assert.NotEqual(course1, course2);
    }
    */
}